{
  "function": "RetryMath.CappedExponentialTerm",
  "_source": "e(n) = min(cap, base*factor^n). Hand-derived; Brooker (AWS 2015) capped-exponential spine.",
  "cases": [
    {"description": "attempt 0 -> base", "inputs": {"base": 1.0, "cap": 30.0, "factor": 2.0, "attempt": 0}, "expected": 1.0, "tolerance": 1e-12},
    {"description": "attempt 1 -> base*2", "inputs": {"base": 1.0, "cap": 30.0, "factor": 2.0, "attempt": 1}, "expected": 2.0, "tolerance": 1e-12},
    {"description": "attempt 2 -> 4", "inputs": {"base": 1.0, "cap": 30.0, "factor": 2.0, "attempt": 2}, "expected": 4.0, "tolerance": 1e-12},
    {"description": "attempt 3 -> 8", "inputs": {"base": 1.0, "cap": 30.0, "factor": 2.0, "attempt": 3}, "expected": 8.0, "tolerance": 1e-12},
    {"description": "attempt 4 -> 16", "inputs": {"base": 1.0, "cap": 30.0, "factor": 2.0, "attempt": 4}, "expected": 16.0, "tolerance": 1e-12},
    {"description": "attempt 5 -> capped (2^5=32 > 30)", "inputs": {"base": 1.0, "cap": 30.0, "factor": 2.0, "attempt": 5}, "expected": 30.0, "tolerance": 1e-12},
    {"description": "attempt 6 -> stays at cap", "inputs": {"base": 1.0, "cap": 30.0, "factor": 2.0, "attempt": 6}, "expected": 30.0, "tolerance": 1e-12},
    {"description": "negative attempt treated as 0", "inputs": {"base": 1.0, "cap": 30.0, "factor": 2.0, "attempt": -1}, "expected": 1.0, "tolerance": 1e-12},
    {"description": "fractional base, small cap", "inputs": {"base": 0.1, "cap": 5.0, "factor": 2.0, "attempt": 3}, "expected": 0.8, "tolerance": 1e-12},
    {"description": "factor 3", "inputs": {"base": 2.0, "cap": 100.0, "factor": 3.0, "attempt": 2}, "expected": 18.0, "tolerance": 1e-12},
    {"description": "factor 1 is a constant schedule", "inputs": {"base": 1.0, "cap": 30.0, "factor": 1.0, "attempt": 5}, "expected": 1.0, "tolerance": 1e-12},
    {"description": "cap binds immediately when base > cap-scaled", "inputs": {"base": 40.0, "cap": 30.0, "factor": 2.0, "attempt": 0}, "expected": 30.0, "tolerance": 1e-12}
  ]
}
